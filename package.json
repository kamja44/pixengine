{
  "name": "pixengine-monorepo",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "description": "A high-performance image optimization engine with policy-based orchestration.",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
