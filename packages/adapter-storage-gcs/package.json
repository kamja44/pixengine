{
  "name": "@pixengine/adapter-storage-gcs",
  "version": "0.1.0",
  "description": "Google Cloud Storage adapter for PixEngine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist",
    "test": "vitest",
    "test:run": "vitest run",
    "prepublishOnly": "pnpm build && pnpm test:run"
  },
  "keywords": [
    "pixengine",
    "image",
    "optimization",
    "storage",
    "gcs",
    "google-cloud-storage"
  ],
  "author": "PixEngine Team",
  "license": "MIT",
  "peerDependencies": {
    "@pixengine/core": "^0.1.0"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.14.0"
  },
  "devDependencies": {
    "@pixengine/core": "workspace:*"
  }
}
