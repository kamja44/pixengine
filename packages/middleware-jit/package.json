{
  "name": "@pixengine/middleware-jit",
  "version": "0.2.0",
  "description": "On-demand image transformation middleware for PixEngine",
  "keywords": [
    "pixengine",
    "express",
    "middleware",
    "image-optimization",
    "jit",
    "on-demand"
  ],
  "author": "PixEngine Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pixengine/pixengine.git",
    "directory": "packages/middleware-jit"
  },
  "bugs": {
    "url": "https://github.com/pixengine/pixengine/issues"
  },
  "homepage": "https://github.com/pixengine/pixengine#readme",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist",
    "test": "vitest",
    "test:run": "vitest run",
    "prepublishOnly": "pnpm build && pnpm test:run"
  },
  "peerDependencies": {
    "@pixengine/core": "^0.1.0",
    "express": "^4.18.0 || ^5.0.0"
  },
  "devDependencies": {
    "@pixengine/core": "workspace:*",
    "@types/express": "^5.0.0",
    "@types/supertest": "^6.0.3",
    "express": "^4.21.2",
    "supertest": "^7.2.2"
  },
  "dependencies": {
    "lru-cache": "^11.2.5"
  }
}
